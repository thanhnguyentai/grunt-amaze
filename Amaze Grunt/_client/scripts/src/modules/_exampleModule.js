define(['jquery', 'underscore'], function ($, _) {

	'use strict';

	var $win = $(window);

	return function Module(opts) {

		var self = this;

		var options = _.extend({

		}, opts);

		function init() {

		};

		init();
	};

});
